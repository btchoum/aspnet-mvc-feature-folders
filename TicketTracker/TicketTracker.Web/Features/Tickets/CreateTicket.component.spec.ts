import { CreateTicketComponent } from './CreateTicket.component';

describe('Create Ticket Component', function () {
    it('should create a simple CreateTicket component', function () {
        const app = new CreateTicketComponent();
        expect(app).toBeDefined();
    });
});
